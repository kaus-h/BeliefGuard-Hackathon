export { e as AddManyItem, A as AddOptions, f as AfterOptions, a as Belief, b as BeliefContext, c as BeliefDelta, d as BeliefSnapshot, B as Beliefs, h as BeliefsConfig, g as BeliefsConfigSchema, C as ClarityChannels, D as DeltaChange, E as Edge, M as Move, T as TraceEntry, W as WorldState, B as beliefs, B as default } from './client-IVkquQfr.js';
import 'zod';

declare class BetaAccessError extends Error {
    readonly code: "BETA_ACCESS_REQUIRED";
    readonly signupUrl = "https://thinkn.ai/waitlist";
    constructor(message?: string);
}
/**
 * Typed SDK error. Parsed from server `{ code, message }` responses.
 */
declare class BeliefsError extends Error {
    readonly code: string;
    readonly retryable: boolean;
    readonly retryAfterMs?: number;
    constructor(code: string, message: string, retryable?: boolean, retryAfterMs?: number);
}

interface BetaAccessRequest {
    email: string;
    name?: string;
    context?: string;
}
interface BetaAccessResponse {
    success: boolean;
    message: string;
}
/**
 * Request beta access to the beliefs SDK.
 *
 * @example
 * ```ts
 * import { requestBetaAccess } from 'beliefs'
 *
 * await requestBetaAccess({
 *   email: 'you@example.com',
 *   name: 'Your Name',
 *   context: 'Building a research agent with Claude',
 * })
 * ```
 */
declare function requestBetaAccess(request: BetaAccessRequest): Promise<BetaAccessResponse>;

export { BeliefsError, BetaAccessError, requestBetaAccess };
